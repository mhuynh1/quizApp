import { createBrowserHistory } from 'history';

/**
 * 
 * 
 * do not use unless accessing props.history
 * is not possible
 * 
 * see for reference: https://codesandbox.io/s/owQ8Wrk3
 */
export default createBrowserHistory();